export type Customer = { id: number; fullName: string; email: string; segment: string; roles: string[]; createdAt: string };
export type TokenResponse = { accessToken: string; refreshToken: string; customer: Customer };
export type Page<T> = { content: T[]; totalElements: number; totalPages: number; number: number; size: number };
export type Product = { id: number; merchantId: number; merchantName: string; name: string; category: string; price: number };
export type Offer = { id: number; title: string; description: string; targetCategory: string; bonusPoints: number; validUntil: string; personalizationScore: number; personalizationReason: string; status: 'AVAILABLE' | 'CLAIMED' | 'USED'; nextStep: string; missionType: string; baselineValue: number; targetValue: number; currentValue: number; progressPercent: number; requiredRating: number; businessGoal: string; missionTitle: string; conditionSummary: string; difficulty: string; metricLabel: string };
export type Balance = { customerId: number; pointsBalance: number; segment: string };
export type Purchase = { id: number; merchantName: string; grossAmount: number; discountAmount: number; totalAmount: number; certificateId?: number | null; certificateCode?: string | null; pointsEarned: number; rewardExplanation?: string | null; purchasedAt: string };
export type Recommendation = { title: string; description: string; source: string };
export type RewardTransaction = { id: number; type: 'EARN' | 'REDEEM' | 'ADJUSTMENT'; points: number; reason: string; createdAt: string };
export type Redemption = { pointsSpent: number; discountAmount: number; remainingBalance: number; confirmationCode: string; rewardName: string; targetCategory: string; personalizationReason: string };
export type Certificate = { id: number; confirmationCode: string; rewardName: string; pointsSpent: number; discountAmount: number; targetCategory: string; status: 'ACTIVE' | 'USED' | 'EXPIRED'; createdAt: string; expiresAt: string; usedAt?: string | null; purchaseId?: number | null };
export type RewardRule = { category: string; cashbackPercent: number; description: string };
export type SegmentRule = { segment: string; minTotalSpend: number; multiplier: number; description: string };
export type LoyaltyProgram = { rewardRules: RewardRule[]; segmentRules: SegmentRule[]; redemptionRule: string };
export type CategoryInsight = { category: string; purchaseCount: number; spent: number; sharePercent: number; lastPurchaseAt?: string | null; averageCheck: number };
export type PurchaseProfile = { favoriteCategories: CategoryInsight[]; purchaseCount: number; totalSpent: number; lastPurchaseAt?: string | null; averageCheck: number; purchaseFrequencyLabel: string; buyerRating: number; buyerRatingLabel: string };

export type AdminStats = { customers: number; products: number; purchases: number; revenue: number; activeCertificates: number; activeOffers: number };
export type AdminUser = { id: number; fullName: string; email: string; segment: string; pointsBalance: number; purchasesCount: number; totalSpent: number; createdAt: string };
export type AdminMerchant = { id: number; name: string; category: string };
export type AdminProduct = { id: number; merchantId: number; merchantName: string; name: string; category: string; price: number };
export type ProductPayload = { merchantId: number; name: string; category: string; price: number };
